# Copy to .env to override docker-compose defaults.
# The API key is shared by the frontend and backend (MVP single-key auth).
API_KEY=dev-api-key

# mock = deterministic canned OCR output (no model download, instant demo)
# paddle = real PaddleOCR (first OCR call downloads ~100 MB of models at runtime)
OCR_MODE=mock